spring:
  application:
    name: vehicle-management-system

  datasource:
    url: jdbc:postgresql://localhost:5432/autosync_db
    username: user
    password: pass123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest